# 项目开发规则

## 需求基线

- 修改前必须完整阅读 `docs/requirements.md` 和相关已有代码。
- 需求文档是产品行为基线；若实现与文档冲突，必须先说明影响，不得静默改变需求。
- 每轮只实现用户明确指定的范围。不得提前加入需求文档“明确不做”的能力，也不得为后续功能预留可见入口。
- 使用原生 Swift 和 SwiftUI，最低支持 macOS 14；只在必要时局部使用 AppKit。
- 所有用户数据必须留在本机，不得接入网络服务、遥测、广告或第三方分析 SDK。

## 项目结构

- `Sources/RandomActionLauncher/`：应用入口和 SwiftUI 界面。
- `Configuration/`：应用包配置和沙盒权限。
- `docs/requirements.md`：已确认的完整需求文档。
- `scripts/`：可重复执行的项目脚本。
- `Tests/`：后续业务逻辑自动化测试；测试结构应与源码模块对应。

## 构建规则

- 日常构建运行 `./scripts/build.sh`，输出位于 `build/RandomActionLauncher.app`。
- 可通过 `CONFIGURATION=release ./scripts/build.sh` 构建发布配置。
- 构建脚本必须可重复执行，不依赖仓库外的生成文件。
- 不得提交 `.build/`、`build/`、`DerivedData/` 或用户级 Xcode 数据。

## 测试与验证规则

- 单元测试是必须的
- 每次修改后至少运行 `./scripts/build.sh`。
- 增加业务逻辑时必须同步增加对应的 Swift Testing 或 XCTest 自动化测试，并运行 `swift test`。
- 涉及窗口、菜单栏、键盘或 VoiceOver 的修改必须补充人工启动验证。
- 构建或测试未通过时不得声称任务完成；应报告失败命令、原因和剩余阻塞。

## 实现边界

- 当前初始化阶段仅允许应用场景、空壳界面、构建配置和工程文档。
- 在后续任务明确授权前，不实现数据模型、持久化、随机算法、冷却、文件导入、安全作用域书签、历史记录或完整项目管理。
- 不创建无需求依据的设置页、统计页、引导页、更新系统或其他附加场景。
- 删除项目记录的实现不得删除、移动或修改用户原始文件。
