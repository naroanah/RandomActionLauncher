# 随机行动启动器实现规格

更新日期：2026-09-22  
状态：开发规格，业务功能尚未实现  
需求基线：[docs/requirements.md](docs/requirements.md) v1.0  
开发进度：[plan.md](plan.md)

## 1. 文档职责与实现边界

需求文档决定产品行为；本文件说明实现方式、模块职责和验收口径；`plan.md` 记录任务状态和验证证据。如三者冲突，以需求文档为准，先说明影响再调整。本文件中的技术选择是后续开发默认方案，不代表代码已实现。

严格沿用需求第 16 节的九步顺序，各阶段结束时应用均应可构建运行。当前任务仅建立文档；后续功能须在获得对应开发授权后实施，遵守 `AGENT.md` 的阶段边界。

- 原生 Swift / SwiftUI，最低 macOS 14，必要时局部桥接 AppKit。
- 项目、书签、冷却和行为记录全部存于本机，不依赖网络。
- 仅访问用户明确选择或拖入的资源，不递归扫描，不请求完全磁盘访问。
- 不实现需求第 3.2 节排除的功能，不为这些功能添加入口。

## 2. 现有工程与目标结构

已检查 `Package.swift`、三个 Swift 源文件、构建脚本和应用配置。当前为 SwiftPM 可执行目标，没有数据库或测试目标。

| 现有文件 | 当前职责 | 后续扩展 |
| --- | --- | --- |
| `RandomActionLauncherApp.swift` | 管理窗口、菜单栏场景 | 注入共享存储和业务协调对象 |
| `MenuBarPanelView.swift` | 抽取占位按钮、管理入口 | 待抽取、结果、空状态、操作反馈 |
| `ProjectManagerView.swift` | 空壳界面 | 列表、添加、编辑、状态管理、重新定位 |
| `scripts/build.sh` | 构建、组装应用包、本地签名 | 保持可重复构建，最终验证发布配置 |
| `Configuration/` | 应用信息与沙盒配置 | 按阶段补充必要文件授权，保持无网络权限 |

计划在 `Sources/RandomActionLauncher/` 下按需增加 `Models/`、`Persistence/`、`Services/`、`Features/`；测试放在 `Tests/RandomActionLauncherTests/`，按对应模块组织。不为目录结构提前创建空模块。

建议职责划分：

| 模块 | 职责 | 可替换依赖 |
| --- | --- | --- |
| 本地仓储 | 模型读写、事务、删除关联数据 | 内存数据库用于测试 |
| 资源访问服务 | 授权、书签解析、重复识别、可用性检查 | 模拟资源解析和访问结果 |
| 项目管理服务 | 添加、编辑、切换状态、删除、重新定位 | 仓储、资源访问服务 |
| 抽取服务 | 候选过滤、加权选择、冷却持久化 | 时钟、随机数源、仓储、资源访问服务 |
| 系统打开服务 | 调用系统默认应用并返回结果 | 模拟成功或失败的打开请求 |
| 会话服务 | 开始、重抽计数、打开记录、结束 | 时钟、仓储 |
| 界面协调对象 | 共享状态、串行操作、结果失效和错误反馈 | 上述服务 |

## 3. 阶段一：数据模型和本地持久化

对应 FR 004、8.1、NFR 002。仓库暂无持久化方案，默认选择 SwiftData，使用磁盘存储作为运行配置、内存存储作为测试配置。数据库位于应用沙盒的 Application Support；初始化失败应显示可恢复的错误，不得静默清空数据。

项目模型必须完整保留以下字段：

| 字段 | 类型 / 约束 |
| --- | --- |
| `id` | UUID，创建后不可变 |
| `displayName` | String，去除首尾空白后不能为空 |
| `note` | String，可空字符串 |
| `resourceType` | folder / video / document |
| `originalPath` | String，完整展示路径 |
| `bookmarkData` | Data，安全作用域书签 |
| `status` | active / paused / completed，默认 active |
| `weight` | low / medium / high，默认 medium；数值 1 / 2 / 3 |
| `createdAt`、`updatedAt` | Date，创建时赋值，修改时更新后者 |
| `lastDrawnAt`、`cooldownUntil` | Date?，新增时为空 |
| `availability` | available / unavailable，与业务状态独立 |

保存成功后再让界面确认变更。保存失败应撤销未提交的内存变更或重新加载已保存状态，明确报告失败。不得仅靠退出时保存。事件模型和会话记录在阶段七实现。

验收：项目字段可写入、读取和更新，重新创建磁盘存储容器后数据不丢失；业务枚举和默认值有单元测试。真实资源的重启访问在阶段二验证。

## 4. 阶段二：选择、拖放、书签和重复检测

对应 FR 002、003、8.2，AC 001、002。

- 文件选择器和拖放共用一个导入服务；允许文件和文件夹，文件夹只创建一个项目。
- 根据资源元数据区分文件夹、视频和文档；不支持或无法打开的资源应拒绝，并说明原因。
- 在用户授权访问期间创建安全作用域书签，持久化后才报告添加成功。解析和使用资源时成对开始、结束安全作用域访问，覆盖成功、失败和取消路径。
- 重复判断优先使用解析后的稳定资源标识，必要时使用规范化 URL；不可只比较名称或未经规范化的路径，不自行假设文件系统大小写规则。
- 重复添加时返回已有项目标识，让管理窗口选中或定位该项；不新增记录。
- 导入时按需补充用户所选文件的只读授权和应用范围书签权限，并验证签名后权限实际生效；不添加网络或全盘权限。

阶段二可加入最小添加入口和选择反馈，完整列表编辑留到阶段三。验收包括两种导入入口、重复识别、文件夹粒度，以及在构建出的沙盒应用中重启后访问书签。

## 5. 阶段三：项目管理

对应 FR 005、006、7.2，AC 007、009。

- 按进行中、暂停、已完成分组；组内按 `updatedAt` 倒序排列。
- 展示名称、完整路径、备注、权重、状态和可用性；支持名称、备注和权重编辑。
- 支持需求第 9.1 节的状态转换；暂停、完成立即退出候选集合。恢复、编辑均不重置冷却。
- 删除前明确提示“只会移除应用中的记录，不会删除本地文件”，确认后删除记录和书签。阶段七接入关联事件清理。
- 所有操作仅更改应用数据，不改动原始资源；阶段六补齐重新定位交互。

验收：编辑和状态变化持久化；删除后原文件内容仍在；错误不造成界面与存储状态不一致。

## 6. 阶段四：候选过滤、加权随机和冷却

对应 FR 007、008、9.3，AC 003、004、007。

一次抽取的处理顺序：

1. 进入共享操作保护，读取最新项目状态和注入时钟的当前时间。
2. 过滤非 active 项目以及 `now < cooldownUntil` 的项目；无冷却或到期的项目继续检查书签和资源可访问性。
3. 按候选权重总和生成 `[0, totalWeight)` 内的整数票号，以累计区间选择一项。
4. 在结果提交时写入 `lastDrawnAt = now`、`cooldownUntil = now + 86400 秒`，保存成功后交付界面展示。保存失败不展示成功结果，并恢复未提交的变更。
5. 释放操作保护。快速重复点击不得产生重叠抽取或重复写入。

随机数源、时钟和资源检查均可注入。概率不得依赖列表排序；默认在抽取内部按不可变 ID 规范排序，方便固定种子测试在不同展示顺序下得到一致结果。单元测试用固定票号验证区间边界，避免以易波动的统计采样作为唯一证据。

使用绝对日期判定连续 24 小时，不使用自然日切换。暂停、完成、恢复、编辑和重新定位均保留冷却。此阶段必须有基础资源可访问性过滤；阶段六补齐真实资源移动、失效状态更新和修复闭环，不允许阶段四把缓存的 available 当作实时访问保证。

## 7. 阶段五：菜单栏抽取闭环

对应 FR 001、009—011、013、015，AC 005、006、012。

- 待抽取状态显示“抽一个”和管理入口；结果显示名称、完整路径、非空备注。
- “打开”和“重抽”同等显眼；允许无限重抽，不要求理由，不增加“跳过”。每次重抽重建候选集合，旧结果保持冷却。
- 使用 `NSWorkspace` 调用系统默认处理方式；文件夹由 Finder 打开。成功打开不改变项目业务状态。
- 打开失败显示错误并保留结果。用户点击打开即结束当前会话；只有系统成功接受请求才计成功打开事件，阶段七接入持久记录。
- 共享协调对象串行处理抽取和重抽，操作期间禁用重复触发；提交前检查面板生命周期，避免面板已关闭仍产生新的可见结果。
- 结果展示期间项目被暂停、完成或删除时刷新或清除结果；打开前再次确认引用和访问有效。
- 关闭管理窗口保持菜单栏运行；关闭菜单栏面板结束当前会话，下次从待抽取状态开始。

空状态至少区分：尚未添加、全部暂停或完成、所有进行中项目冷却、所有进行中项目路径不可用；均有管理入口，不提供强制绕过冷却。冷却与不可用混合时说明两类原因；同一项目同时满足两者时允许重复计数，不错误宣称只有单一原因。该混合说明为实现补充，不改变候选条件。

## 8. 阶段六：路径失效、自动回退和重新定位

对应 FR 012，AC 008。

- 抽取前重新验证资源，不能永久跳过上次标记为 unavailable 的项目，保证磁盘重新挂载后可恢复。
- 书签能解析移动后的资源时更新完整路径；书签过期时尝试重新生成并保存，再继续使用。
- 无法解析或访问时保存 unavailable，从本次候选集合排除并继续处理其他项目；不增加用户重抽次数。
- 管理窗口显示不可用状态和“重新定位”。重新选择后复用导入校验和重复识别，成功则更新类型、路径、书签和可用性，保留项目 ID、业务状态与冷却。
- 重新定位指向另一个已有项目时拒绝重复绑定并定位已有项目；取消或失败保留原记录。

验收覆盖移动、删除、外接磁盘卸载和重新挂载，以及重新定位成功、取消和冲突。单个失效项目不能阻断其余候选。

## 9. 阶段七：最少本地行为记录

对应 FR 014、015，AC 011。只记录必要事件，不提供统计界面。

会话字段：`id: UUID`、`startedAt: Date`、`rerollCount: Int`、`openedProjectId: UUID?`、`openedAt: Date?`、`timeToOpenMs: Int?`。新会话重抽数为零，其余可空字段为空。

- 首次主动点击“抽一个”创建并保存会话，即使没有候选也保留这次主动开始记录。
- 每次被接受的用户重抽操作增加一次计数，包括重抽后候选为空；被并发保护挡住的点击不计数，内部失效回退不计数。
- 点击打开、面板关闭或应用退出结束会话；下一次主动开始创建新会话。失败打开后保留结果供重试，但不得重复结束或重复写入同一次操作。
- 实现补充：成功打开后填充首次成功打开项目、点击时间和耗时；耗时采用 `max(0, 点击时间 - startedAt)` 的毫秒数。失败请求保留成功打开字段为空。因需求同时要求“是否点击过打开”和“成功打开事件”，另存 `didClickOpen: Bool`，区分失败点击与未点击；不扩大采集范围。
- 为完成 FR 006 的关联清理，维护仅含会话 ID、项目 ID 的内部关联，覆盖该会话展示过的项目；删除项目时删除其关联的整条会话及关联记录，其他项目记录不受影响。此为实现默认清理策略，不保存额外内容快照。
- 会话变更即时保存；异常退出不丢失已提交记录，不推测补写未发生的打开。

## 10. 阶段八、九：验证与交付

自动化测试随业务阶段同步增加，阶段八集中补齐交叉场景，不得推迟所有测试到末尾。测试目标使用 Swift Testing 或 XCTest；覆盖需求第 13 节全部十类自动化测试，并补充持久化失败、系统打开失败、失效结果引用、混合空状态和时钟回拨。

每次修改至少运行 `./scripts/build.sh`；新增或修改业务逻辑同时运行 `swift test`。窗口、菜单栏、键盘和 VoiceOver 修改必须人工启动验证。纯文档修改不以新增无意义测试替代构建检查。

最终执行 `CONFIGURATION=release ./scripts/build.sh`，验证应用包签名、沙盒权限和从个人安装位置启动，按 AC 001—012 逐项记录证据。人工检查拖放、选择器、重启书签访问、移动文件、外接磁盘、完整抽取流程、键盘、VoiceOver、浅深色和断网使用。15—30 个项目下检查面板响应与抽取体验。

MVP 完成必须满足需求第 14 节全部七项条件。构建成功、已有界面空壳或单项测试通过均不能代替完整验收。
