# 随机行动启动器开发计划与进度

更新日期：2026-09-22  
需求基线：[docs/requirements.md](docs/requirements.md) 第 16 节  
实现规格：[spec.md](spec.md)

## 1. 当前进度与维护规则

当前已完成数据模型与本地持久化，并已实现 S2 的资源导入、书签、重复检测和管理窗口最小添加界面。S2 自动化、构建、签名、文件夹选择器导入及重启保留已通过；视频、文档和拖放仍需补充人工验证，因此阶段保持“待验证”。S3 的项目管理服务、列表编辑、状态切换、删除引用及对应自动化测试已实现，因窗口操作尚未人工验证保持“待验证”。S4 的候选过滤、权重随机、冷却和依赖注入测试已完成。

第 16 节业务阶段完成数：**2 / 9**。下一开发阶段为 **S5 菜单栏面板、结果卡片、无限重抽和系统打开**；S2 视频、文档和拖放以及 S3 列表操作与重启字段保留仍需补充人工验证。

- 状态取值：未开始、进行中、待验证、已完成、阻塞。
- 实现存在但缺少必需验证时只能标记“待验证”；阻塞时记录原因及解除条件。
- `[x]` 只用于有证据完成的任务，不能因写入计划就勾选。
- 每完成一个阶段，同步更新本表、任务勾选和验证记录；涉及规格调整时先核对需求再更新 `spec.md`。
- 测试与实现同步提交；每阶段保持应用可运行，不提前加入下一阶段的完整功能或无需求入口。

| 阶段 | 对应第 16 节 | 依赖 | 状态 | 验证证据 |
| --- | --- | --- | --- | --- |
| S1 | 数据模型和本地持久化 | 当前工程 | 已完成 | 7 项自动化测试、调试构建及人工启动通过 |
| S2 | 文件选择、拖放、书签、重复检测 | S1 | 待验证 | 28 项总测试通过；调试构建与最小沙盒授权签名通过；文件夹选择器导入、单条记录和重启保留人工通过 |
| S3 | 列表、编辑、状态、删除引用 | S2 | 待验证 | 34 项完整测试通过，其中 6 项覆盖 S3；调试构建通过；人工界面验证未完成 |
| S4 | 候选过滤、随机、冷却 | S3 | 已完成 | 42 项完整测试通过，其中 8 项覆盖 S4；调试构建通过；真实结果交互按计划留到 S5 |
| S5 | 菜单栏、结果、重抽、系统打开 | S4 | 未开始 | — |
| S6 | 路径失效、回退、重新定位 | S5 | 未开始 | — |
| S7 | 最少本地行为记录 | S6 | 未开始 | — |
| S8 | 测试、键盘、VoiceOver、异常 | S7 | 未开始 | — |
| S9 | 个人安装构建、逐项验收 | S8 | 未开始 | — |

## 2. 分阶段执行清单

### S1 数据模型和本地持久化

交付位置：`Models/`、`Persistence/`、应用入口、`Package.swift`、对应测试目录。规格见 spec 第 3 节。

- [x] 建立项目模型及资源类型、状态、权重、可用性枚举，覆盖 FR 004 全部字段。
- [x] 建立 Core Data 本地仓储和共享容器，区分磁盘运行存储与内存测试存储。
- [x] 实现基础增删改查、显式保存及失败恢复，不接入真实资源导入界面。
- [x] 增加测试目标，验证默认值、字段往返、更新和重新加载后的持久化。
- [x] 构建和测试通过，启动现有空壳验证存储初始化不影响场景。

完成条件：应用可运行，本地存储可稳定读写；不能把测试数据写入正式用户数据。AC 001 的真实导入闭环待 S2 / S3 完成。

### S2 文件选择、拖放、安全作用域书签和重复检测

交付位置：资源访问与导入服务、管理窗口最小添加入口、沙盒配置及对应测试。规格见 spec 第 4 节。

- [x] 实现支持文件和文件夹的系统选择器，以及复用导入流程的拖放入口。
- [x] 实现类型检查、默认名称、默认状态和权重；拒绝不支持或不可打开的资源。
- [x] 实现安全作用域书签创建、解析和访问生命周期；补充所需最小授权。
- [x] 实现规范资源标识和重复识别，返回已有项目并定位，不递归扫描文件夹。
- [x] 自动化覆盖重复、不同资源、无效资源、授权失败，以及书签往返和移动后解析。系统选择器取消由无副作用分支实现，待随剩余人工流程复验。
- [ ] 在沙盒应用中手工导入文件、视频、文件夹，重启验证书签仍可访问。
- [ ] 构建、测试通过并记录人工验证结果。

完成条件：AC 001 的添加与重启保留、AC 002 通过；完整字段编辑验证在 S3 补齐。

### S3 项目列表、编辑、状态切换和删除引用

交付位置：项目管理服务、管理界面及对应测试。规格见 spec 第 5 节。

- [x] 展示全部字段，按业务状态分组、最近更新倒序排列。
- [x] 实现名称、备注、权重编辑和空名称校验。
- [x] 实现暂停、恢复、完成、重新激活，保留已有冷却字段。
- [x] 实现删除确认和记录删除，明确原文件不受影响。
- [x] 自动化覆盖状态转换、编辑持久化、删除后原文件仍存在。
- [ ] 构建、测试通过，人工验证列表操作和重启后的字段保留。

完成条件：项目可维护，AC 001 完整字段验证和 AC 009 的原文件保护通过；关联事件删除在 S7 接入后复验。

### S4 候选过滤、权重随机和 24 小时冷却

交付位置：抽取服务、可注入时钟和随机源、对应测试。规格见 spec 第 6 节。

- [x] 按 active、冷却已到期、实时资源可访问检查构建最新候选集合，不依赖 availability 缓存。
- [x] 实现 1 / 2 / 3 权重映射和整数票号选择，与界面排序解耦。
- [x] 实现结果提交、连续 24 小时冷却和保存失败恢复。
- [x] 实现 MainActor 串行/重入保护，避免快速操作造成重叠写入。
- [x] 自动化覆盖过滤、权重区间、固定票号、列表重排、冷却前后边界、时钟变化和重入。
- [x] 调试构建和 42 项完整测试通过，保持现有界面可启动；真实结果交互留到 S5 接入。

完成条件：AC 003、004、007 的逻辑验证通过，资源可访问性不能仅依赖历史缓存。

### S5 菜单栏面板、结果卡片、无限重抽和系统打开

交付位置：菜单栏界面、共享协调对象、系统打开服务。规格见 spec 第 7 节。

- [ ] 将占位面板替换为待抽取、结果、空状态和错误反馈。
- [ ] 展示完整路径、可换行名称和非空备注，“打开”与“重抽”同等显眼。
- [ ] 接入抽取和无限重抽、重复点击保护及面板生命周期。
- [ ] 接入系统默认应用打开；失败保留结果，成功不标记完成。
- [ ] 实现四种必需空状态和混合原因，均提供管理入口。
- [ ] 在项目暂停、完成或删除时刷新当前结果，阻止打开失效引用。
- [ ] 自动化验证协调逻辑、空候选、打开失败和结果失效。
- [ ] 构建、测试通过，人工验证菜单栏闭环及关闭管理窗口后继续运行。

完成条件：AC 005、006、012 通过，可运行完整抽取闭环；行为记录在 S7 持久化。

### S6 路径失效检测、自动回退和重新定位

交付位置：资源访问服务、抽取服务、管理界面。规格见 spec 第 8 节。

- [ ] 完善移动后书签解析、过期书签刷新及新路径保存。
- [ ] 标记失效资源并继续其他候选，磁盘恢复后可重新判断可用性。
- [ ] 增加不可用提示和重新定位，保留原 ID、状态与冷却。
- [ ] 实现重新定位取消、失败和重复资源冲突处理。
- [ ] 自动化覆盖解析失败、自动回退、恢复可用和重新定位边界。
- [ ] 人工验证移动文件、删除文件、外接磁盘断开与恢复。
- [ ] 构建和测试通过，记录沙盒中书签行为的实测结果。

完成条件：AC 008 通过，单个失效项目不阻断抽取；失败回退不算用户重抽。

### S7 最少本地行为记录

交付位置：会话模型、会话服务、仓储、界面协调对象。规格见 spec 第 9 节。

- [ ] 增加本地会话模型、点击打开标记及最少会话项目关联。
- [ ] 接入首次抽取、重抽计数、点击打开、成功打开和会话结束。
- [ ] 处理打开失败后重试、面板关闭、正常退出、异常退出和时钟回拨。
- [ ] 删除项目时同步清理关联本地会话数据，不影响原文件。
- [ ] 自动化覆盖计数、非负耗时、失败与成功区分、幂等处理和关联删除。
- [ ] 构建、测试通过，验证重启后已提交数据仍存在且无统计入口。

完成条件：AC 011 通过，复验 AC 009；所有事件仅保存在本机。

### S8 补齐自动化测试、键盘操作、VoiceOver 和异常处理

交付位置：对应业务测试、界面可访问性配置和异常处理。规格见 spec 第 10 节。

- [ ] 核对需求第 13 节十类自动化测试，补齐跨模块遗漏。
- [ ] 覆盖持久化失败、连续点击、失效引用、混合空状态和时间变化。
- [ ] 补齐键盘焦点、操作路径、VoiceOver 标签和非颜色状态提示。
- [ ] 人工完成抽取、重抽、打开、添加和基本管理的键盘 / VoiceOver 流程。
- [ ] 检查浅深色、长名称、长路径、空备注以及 15—30 个项目的响应。
- [ ] 构建、完整测试通过；记录人工通过项、失败项和修复结果。

完成条件：需求第 13 节测试清单全部有结果，失败项修复并回归；未做的人工检查不得标记通过。

### S9 个人安装构建与逐项验收

交付位置：发布应用包、验收记录；按需要增加可重复执行的安装说明。

- [ ] 执行 `CONFIGURATION=release ./scripts/build.sh`，检查应用签名与最小沙盒权限。
- [ ] 从个人安装位置启动，验证菜单栏、管理窗口和重启数据保留。
- [ ] 在断网环境验证核心功能，检查代码无遥测、网络服务或分析 SDK。
- [ ] 执行下方 AC 001—012 验收并填写证据，修复失败项并回归。
- [ ] 核对需求第 14 节全部七项 MVP 完成条件。
- [ ] 更新版本、构建信息、已知限制与最终验证记录。

完成条件：全部必需验收通过，存在可安装运行的个人构建。需求第 15 节两周自用观察为后续建议，不作为本阶段阻塞条件。

## 3. 验收追踪表

| 验收项 | 主要阶段 | 验证方式 | 当前结果 / 证据 |
| --- | --- | --- | --- |
| AC 001 添加与持久化 | S1—S3 | 模型测试 + 三类资源编辑后重启 | 未验证 |
| AC 002 文件夹粒度 | S2 | 导入测试 + 文件夹内有子项的人工检查 | 已验证：含子文件的 `CS61A-Assignments` 仅创建一条文件夹记录 |
| AC 003 加权随机 | S4 | 固定票号、票号边界、重排测试 | 已验证：S4 自动化覆盖 1 / 2 / 3 区间和仓储顺序无关性 |
| AC 004 冷却 | S4 | 注入时钟，验证 24 小时边界和保存 | 已验证：S4 自动化覆盖未到期、等于边界、回拨和精确 86400 秒保存 |
| AC 005 无限重抽 | S5 | 协调测试 + 连续重抽操作 | 未验证 |
| AC 006 系统打开 | S5 | 打开服务测试 + 默认应用人工检查 | 未验证 |
| AC 007 状态过滤 | S3、S4 | 状态转换与候选过滤测试 | 已验证：S3 状态转换与 S4 active 过滤自动化通过 |
| AC 008 失效路径 | S6 | 回退测试 + 移动、卸载与重新定位 | 未验证 |
| AC 009 安全删除 | S3、S7 | 原文件存在 / 内容测试 + 关联数据清理 | S3 原文件保护自动化通过；关联事件清理待 S7 |
| AC 010 隐私 | S9 | 代码和权限检查 + 断网运行 | 未验证 |
| AC 011 最少行为记录 | S7 | 会话测试 + 本地存储核对 | 未验证 |
| AC 012 空状态 | S5、S6 | 四种及混合原因测试 + 管理入口 | 未验证 |

## 4. 验证记录与后续更新

每条记录填写：日期、阶段 / 任务、检查方式、实际结果、证据位置、遗留问题。构建通过只证明当前工程可构建，不改变未实现业务阶段的状态。

| 日期 | 范围 | 检查方式 | 结果 | 遗留事项 |
| --- | --- | --- | --- | --- |
| 2026-09-22 | 规划基线 | 阅读需求、项目规则、全部已有源码和配置 | 确认仅有工程空壳；九阶段尚未实现 | 后续按授权从 S1 开始 |
| 2026-09-22 | 文档建立 | `./scripts/build.sh`；阶段、验收编号和相对链接检查 | 调试构建与本地签名通过；九阶段、十二项验收和文档链接完整 | 本轮仅改文档；未运行业务测试，当前工程尚无测试目标；未进行人工界面验收 |
| 2026-09-22 | S1 数据模型和本地持久化 | `./scripts/test.sh`；`./scripts/build.sh`；启动构建产物检查管理窗口 | 7 项测试全部通过；调试应用构建、签名并正常显示管理窗口 | 真实书签与资源重启访问留待 S2；完整字段编辑留待 S3 |
| 2026-09-22 | S2 资源导入实现与部分人工验证 | `./scripts/test.sh`；`./scripts/build.sh`；签名授权检查；系统选择器导入含子文件的文件夹；结束并重启应用 | 28 项测试通过；调试构建通过；仅含沙盒、应用范围书签和用户选择只读权限；文件夹只生成一条记录，重启后仍显示 | 当前 UI 自动化无法继续向前台窗口发送点击；待人工补验视频、文档、拖放、重复定位和选择器取消后将 S2 标为完成 |
| 2026-09-22 | S3 项目列表、编辑、状态、删除引用 | 新增 `ProjectManagementTests`；在可访问安全书签的环境运行 `./scripts/test.sh`；`./scripts/build.sh`；代码边界审查 | 34 项完整测试通过，其中 6 项覆盖分组排序、编辑持久化、空名称失败、5 条合法状态边、冷却字段保留、删除原文件保护和存储失败恢复；调试构建通过；未增加 S4 抽取、S6 重新定位或 S7 事件逻辑 | 未进行人工窗口/重启界面验证，S3 保持“待验证” |
| 2026-09-22 | S4 候选过滤、权重随机和 24 小时冷却 | 在可访问安全书签的环境运行 `./scripts/test.sh`；`./scripts/build.sh`；`git diff --check`；代码边界审查 | 42 项完整测试通过，其中 8 项覆盖状态/冷却/实时资源检查、不可用缓存恢复、权重区间、仓储顺序、精确冷却时间、保存失败、时钟回拨和重入保护；调试构建与 diff 检查通过；未接入 S5 UI、S6 路径修复或 S7 会话 | 无 |

后续每轮开发结束时，记录实际修改及测试结果，勾选已完成任务，并明确下一个未完成任务。因 SDK、权限、测试设备或人工验证条件阻塞时，保留未完成状态并写明解除条件，不跳过验收。
